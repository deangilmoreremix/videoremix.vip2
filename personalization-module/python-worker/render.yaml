services:
  - type: web
    name: videoremix-personalizer-worker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PERSONALIZER_WORKER_KEY
        sync: false
