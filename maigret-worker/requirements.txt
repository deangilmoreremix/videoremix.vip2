fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
# NOTE: maigret is NOT pinned here. Public PyPI only publishes maigret up to
# 0.4.4 and the 0.5.0/0.6.3 versions referenced historically are not on the
# public index (they resolve to a Rust/maturin sdist that fails to build on
# Render's read-only filesystem). The scanner imports maigret defensively and
# degrades to an empty result when it is unavailable, so the worker builds and
# serves /health + /scan (with a seed-only graph) regardless. To enable real
# Maigret scans, install maigret from its source repo or a private wheel that
# provides a prebuilt binary for the target Python.
