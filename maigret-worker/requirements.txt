fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
# NOTE: maigret is intentionally NOT pinned. Public PyPI only publishes maigret
# up to 0.4.4; the 0.5.0/0.6.3 versions historically referenced are not on the
# public index and resolve to a Rust/maturin sdist that fails to build on Render's
# read-only filesystem. The scanner imports maigret defensively and degrades to an
# empty result when it is unavailable, so the worker still builds and serves
# /health + /scan (with a seed-only graph). To enable real Maigret scans, install
# maigret from its source repo or a private wheel for the target Python.
#
# The service is pinned to Python 3.12 via PYTHON_VERSION (see render.yaml) so
# pydantic-core / uvicorn have prebuilt wheels. On Render's default Python 3.14
# those wheels are absent and the build fails.
