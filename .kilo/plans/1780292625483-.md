# Plan: Understanding & Managing Branch Divergence

## Current State Analysis

### Repository Status
- **Working tree**: Clean (no uncommitted changes)
- **Local branch**: `session/agent_b7b2cbaf-c229-43e9-b23c-2145cac6c298` at `1ae0a3ba`
- **Remote branches**: 13 total (main, master + 11 session branches)

### The 643 "Missing" Commits
These are commits on `origin/main` that don't exist on `origin/master`. They represent:

1. **Merged session work** (already integrated):
   - `feature/landing-page-update` → merged into main
   - `feat/update-impeccable-website` → merged into main (current HEAD)
   - ~10 session branches with PRs that were merged

2. **Only 38 commits remain unmerged** on session branches:
   - Personalizer work (agent_be67f86a)
   - Data cleanup work (agent_cbe39e94)
   - Diagnostic tools (agent_e6b385e8)
   - Various WIP/cleanup commits

### Branch Structure
```
origin/main (current working branch)
├── Has 643 commits ahead of master
├── Contains merged feature/landing-page-update
└── Contains merged feat/update-impeccable-website

origin/master (older branch)
└── Last commit: May 9, 2026 (render.yaml)
```

## Questions for Clarification

Before creating PRs and merging, need to understand:

1. **What is the intended branch strategy?**
   - Should `master` be used as the primary branch?
   - Should `main` be merged into `master`?
   - Or should `master` be deleted/archived?

2. **For the 38 unmerged commits:**
   - Should each session branch be turned into a PR?
   - Are they ready for merge, or need review first?

3. **For the 643 merged commits:**
   - Are they already deployed to production?
   - Do they need to be synced to `master`?

## Proposed Next Steps

### Option A: Merge main into master
```bash
# Create PR from main to master
gh pr create --base master --head main --title "Merge main: landing page updates, impeccable integration, personalization features"
```

### Option B: Convert unmerged session branches to PRs
For each unmerged session branch:
```bash
# Create PR for pending session work
gh pr create --base main --head origin/session/agent_XXXXX --title "..."
```

## Session Branch PRs (to be created)

### 11 Unmerged Session Branches - Recommended PRs

| Branch | Commits | Last Commit | Summary |
|--------|---------|-------------|---------|
| agent_be67f86a | 11 | May 31 | Personalizer: UI visibility fix, Python worker, scan/intelligence features |
| agent_cbe39e94 | 5 | May 28 | Data cleanup: Remove unused agent apps from appsData |
| agent_bcfc0b58 | 6 | May 27 | Cache-busting, pnpm workspace config, dotenv updates |
| agent_e6b385e8 | 4 | May 25 | Diagnostic tools for dynamic imports and HMR |
| agent_1013635c | 4 | May 4 | Wallet icon fix, VSCode HMR, app IDs customer-friendly |
| agent_29135046 | 4 | Apr 25 | Authentication system overhaul (WIP) |
| agent_3d28e472 | 4 | Apr 25 | App interaction flow plan updates |
| agent_59179909 | 4 | Apr 25 | Auth logging and sync (WIP) |

### Recommendation: master is deprecated
- `master` last updated May 9, 2026 by Dean
- `main` last updated May 31, 2026 by Shashee  
- The 643 commits on `main` represent significant newer work
- **Action**: Archive `master`, use `main` as primary branch

## Implementation Steps

### Phase 1: Create PRs for Session Branches
```bash
# Already on session/agent_b7b2cbaf, push to main
git push origin HEAD:main

# Create PRs for each unmerged session branch
gh pr create --base main --head origin/session/agent_be67f86a --title "feat(personalizer): UI visibility fix and Python worker integration" --body "Includes scan job management, intelligence pipeline, identity graph UI"
gh pr create --base main --head origin/session/agent_cbe39e94 --title "chore(data): Remove unused agent apps from appsData"
gh pr create --base main --head origin/session/agent_bcfc0b58 --title "fix(deps): Cache-busting and pnpm workspace configuration"
gh pr create --base main --head origin/session/agent_e6b385e8 --title "chore: Add diagnostic tools for dynamic imports and HMR"
gh pr create --base main --head origin/session/agent_1013635c --title "fix(ui): VSCode HMR and customer-friendly app IDs"
gh pr create --base main --head origin/session/agent_29135046 --title "feat(auth): Comprehensive user authentication system"
gh pr create --base main --head origin/session/agent_3d28e472 --title "chore(config): Update app interaction flow plan"
gh pr create --base main --head origin/session/agent_59179909 --title "chore(auth): Debug logging and auth flow sync"
```

### Phase 2: Reconcile main vs master
- **Option A**: Create PR `main → master` for full sync (not recommended - loses newer work)
- **Option B**: Archive `master`, continue using `main` (recommended)