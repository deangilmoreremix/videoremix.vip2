# Plan: Add 12 Grouped App Categories to Dashboard & Nav Header Tools Dropdown

## Context
- No prior work exists: last commit is unrelated (GTM content fixes), working tree clean, no feature branches
- Existing apps stored in Supabase `apps` table + local fallback `appsData.ts`
- Current dashboard (`DashboardToolsSection.tsx`) uses flat `toolCategories` (4 categories) filtering by `app.category`
- Current nav header (`SpecialHeader.tsx`) uses flat `featuredTools` list with no grouping
- Goal: Group apps by 12 user-defined categories, display grouped apps on dashboard and nav tools dropdown, integrate superpowers skills

## 1. Data Layer Updates
### 1.1 Extend App Data Interfaces
- Update `App` interface in `src/data/appsData.ts` to add:
  - `group: string` (12 category IDs)
  - `skillSlug: string` (superpowers skill reference)
  - `productName: string` (VideoRemix Product Name)
  - `targetAudience: string` (Who Would Buy It)
- Update `ComponentApp` interface in `src/utils/appTransformers.ts` to include same new fields
- Update `DatabaseApp` interface in `src/utils/appTransformers.ts` to include `group` column

### 1.2 Supabase Migration
- Create new migration to add `group` column to `apps` table
- Add `skill_slug` column to `apps` table for superpowers integration

### 1.3 App Group Definitions
- Create `src/data/appGroups.ts` with 12 user-provided categories:
  ```typescript
  interface AppGroup {
    id: string;
    label: string;
    icon: React.ReactNode;
    offerAngle: string;
  }
  ```
- Map each group to user-provided categories (e.g., `sales-lead-gen`, `content-creation`, etc.)
- Use existing lucide-react icons matching group themes

### 1.4 Populate App Data
- Update `appsData.ts` or Supabase `apps` table to assign each app to its corresponding `group`
- Add `skillSlug` mapping to superpowers skills for each app
- Add `productName` and `targetAudience` from user-provided list

## 2. Dashboard Apps Display Update (`src/components/dashboard/DashboardToolsSection.tsx`)
### 2.1 Replace Category Filter
- Replace existing `toolCategories` with imported `appGroups` data
- Update `handleCategoryChange` and filtering logic to filter by `app.group` instead of `app.category`

### 2.2 UI Updates
- Keep existing category filter tabs, now showing 12 groups
- Update app card rendering to optionally show group context
- Preserve existing features (featured apps, search, animations)

## 3. Nav Header Tools Dropdown Update (`src/components/SpecialHeader.tsx`)
### 3.1 Group Featured Tools
- Replace flat `featuredTools` array with grouped structure:
  ```typescript
  const groupedTools = appGroups.map(group => ({
    ...group,
    tools: featuredTools.filter(tool => tool.group === group.id)
  }))
  ```

### 3.2 Dropdown Rendering
- Update dropdown to render groups with section headers
- Use collapsible sections or nested grid per group
- Maintain existing UI patterns (dark theme, backdrop blur, 2-col grid per group)
- Keep "Browse All Tools" link

## 4. Superpowers Skills Integration
- Map each app's `skillSlug` to corresponding superpowers skill from repo
- Ensure skills are correctly referenced for "using superpowers skills" requirement
- Add skill metadata to app detail modals if needed

## 5. Validation
- Run typecheck: `npm run typecheck`
- Run lint: `npm run lint`
- Test dashboard app grouping renders correctly
- Test nav dropdown shows grouped tools
- Verify no regressions to existing app functionality
