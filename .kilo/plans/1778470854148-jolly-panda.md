# Plan: Rebuild Landing Page Design from Impeccable Commits Only

## Objective
Extract and reapply only the **design-related commits and files** to rebuild the landing page with impeccable backgrounds, animations, and premium features. Focus on visual design, not infrastructure/bug fixes.

---

## Design-Only Commit Timeline

### Design Foundation
- `23ef92c5` - feat: Add impeccable design skill and reference library
  - Added `.agents/skills/impeccable/` with design reference library (30+ files)
  - Added SKILL.md defining impeccable methodology

### Premium Design Components (Visual Assets)
- `61bb9534` - Restore impeccable skills landing page with premium components
  - **Added 19 premium visual design components** (ParticleBackground, ROICalculator, PersonalizationSimulator, AnimatedTestimonialCard, LiveActivityFeed, LogoWall, ParallaxSection, AnimatedBorderGradient, etc.)
  - **Updated LandingPage.tsx** to integrate these design components
  - This is the **pristine design state** to aim for

### Design Transformation & Polish
- `3ebea878` - feat: Transform landing page with premium animations and interactive features
  - Enhanced animation performance and visual polish
  - Updated all statistics with 2026 research (Engagement: 2.3x, Conversion: 80%, ROI: 5-8x)
  - Changed "Video" → "Marketing" terminology throughout
  - Added 22 premium animation/interactive components
  - **This is the enhanced, final design state**

### Build & Style Configuration
- `1efe5f80` - Update site with impeccable skills improvements
  - Updated Netlify config, Vite config
  - Updated CSS/index styles to align with design system

### Final Cleanup
- `247a3945` / `ba66dda4` - Update site with impeccable skills improvements and clean up unused files
  - Final cleanup of design-adjacent files

---

## Design Files to Extract from Git

### CSS/STYLE FILES
1. `src/index.css` (design base layer) - from commit 61bb9534 (pristine) or 3ebea878 (enhanced)
2. Any `*.css` files (check for AnimatedBackground.css)

### REACT DESIGN COMPONENTS
**Premium Components** (`src/components/premium/`):
- ParticleBackground.tsx
- ROICalculator.tsx
- PersonalizationSimulator.tsx
- InteractiveComparisonTable.tsx
- AnimatedTestimonialCard.tsx
- LiveActivityFeed.tsx
- LogoWall.tsx
- ParallaxSection.tsx
- AnimatedBorderGradient.tsx
- +9 more (see full list below)

**Updated Core Components**:
- `SpecialHero.tsx` (updated in 1efe5f80)
- `SEO.tsx` (added in 1efe5f80)

**Page Files**:
- `src/pages/LandingPage.tsx` - the integration point

**Hooks** (animation infrastructure):
- `src/hooks/useScrollAnimation.ts`
- `src/hooks/useReducedMotion.ts`

**Context**:
- `src/context/AnimationContext.tsx`

### CONFIG FILES (Design System)
- `tailwind.config.js` - verify brand colors (indigo/violet/pink)
- `vite.config.ts` - for build config
- `netlify.toml` - deployment (already correct)

---

## Extraction Plan

### 1. List All Design-Related Commits
Get the full chronological list of design-only commits and extract their file changes.

```
git log --oneline --all | grep -iE "impeccable|premium|design|style|polish|animate|hero" | head -20
```

### 2. Identify Pristine Versions of Design Files
For each key design file, GET both the pristine version (from 61bb9534 - "Restore impeccable skills landing page") and the enhanced version (from 3ebea878 - "Transform landing page"):
- `src/components/premium/ParticleBackground.tsx`
- `src/components/premium/ROICalculator.tsx`
- `src/components/premium/PersonalizationSimulator.tsx`
- `src/components/premium/AnimatedBorderGradient.tsx`
- `src/components/premium/ParallaxSection.tsx`
- `src/components/premium/InteractiveComparisonTable.tsx`
- `src/components/premium/AnimatedTestimonialCard.tsx`
- `src/components/premium/LiveActivityFeed.tsx`
- `src/components/premium/LogoWall.tsx`
- All others in `src/components/premium/`
- `src/index.css` (both versions)
- `src/pages/LandingPage.tsx` (both versions)
- `src/hooks/useScrollAnimation.ts` & `useReducedMotion.ts`

Save these to `/tmp/impeccable-pristine/` and `/tmp/impeccable-enhanced/` for reference.

### 3. Compare Pristine vs Enhanced vs Current
Create a diff report:
- What changed between pristine and enhanced design versions?
- What changed between enhanced and current HEAD?
- Identify what has been lost/corrupted/drifted
- Identify any conflicting changes

This will tell us exactly what needs to be restored.

### 4. Decide Restoration Strategy
**Two paths:**
- **Path A (Conservative):** Current HEAD already has most enhancements → only fix what's missing/drifted
- **Path B (Clean slate):** Rebuild from pristine to ensure no corruption → apply 3ebea878 changes cleanly

User must choose which path.

### 5. Apply Restoration (Based on User Choice)
If Path A: Restore specific missing files from pristine commit
If Path B: 
- Replace current premium components with pristine versions from 61bb9534
- Re-apply the "transform" commit 3ebea878 changes manually
- Reapply 1efe5f80 style updates

### 6. Fix Text Content
Ensure all statistics and copy match the updated 2026 data:
- Engagement: **2.3x** (Markettailor) [was "215%"]
- Conversion: **80%** (Monetate) [was "183%"]
- ROI: **5-8x** (McKinsey) [was "247%"]
- CTA conversion: **2.0x** (Markettailor) [was "287%"]
- Replace "Video" → "Marketing" where appropriate

### 7. Rebuild `src/index.css` from Impeccable Principles
If CSS is corrupted, rebuild fresh:
- Use Tailwind @apply with brand tokens (not arbitrary colors)
- Implement glass morphism with brand-tinted colors (no pure white)
- Use indigo/violet/pink gradient, not arbitrary colors
- Include all animations (float, shimmer, gradient-shift, etc.)
- Include all background effects (film-grain, grid, particles)
- Ensure `prefers-reduced-motion` guardrails
- Ensure no pure #000/#fff in neutral colors (tint toward brand hue)

### 8. Verify LandingPage.tsx Integration
Must have:
- ParticleBackground (className="absolute inset-0 z-0" or similar)
- ROI Calculator section
- PersonalizationSimulator with ParallaxSection
- LiveActivityFeed (fixed bottom-right)
- LogoWall section
- Proper lazy loading (Suspense) for all lazy imports
- Error boundaries
- Vertical rhythm with varied spacing (not uniform)

### 9. Build and Verify
```
npm run build
```
Check dist for:
- All premium component code in bundle
- All CSS classes present
- No missing module errors

### 10. Commit with Design-Focused Message
```
git commit -m "feat(design): restore impeccable landing page design with premium components

- Restore premium components from commit 61bb9534 (ParticleBackground, ROICalculator, etc.)
- Reapply transform enhancements from commit 3ebea878
- Update statistics to 2026 research-backed data
- Rebuild index.css using impeccable design principles
- Fix text: Video→Marketing, updated stats"
```

---

## Deliverables

1. **All 19 premium design components** present in `src/components/premium/` (pulled from pristine git state)
2. **LandingPage.tsx** correctly integrates all premium components
3. **src/index.css** rebuilt using impeccable methodology (brand-tinted, glass morphism, proper gradients)
4. **Hooks and context** working (useScrollAnimation, useReducedMotion, AnimationContext)
5. **Updated copy** with correct 2026 statistics and "Marketing" terminology
6. **Build succeeds** with no errors

---

## Questions for User

1. **Do you want Path A (fix specific missing pieces) or Path B (clean slate rebuild from pristine)?**

2. **What specific design features are NOT loading for you?** (e.g., background animations, gradient backgrounds, glass effects, hover effects, particle canvas, parallax scrolling)
   → This will help identify which files to prioritize restoring.

3. **What does "content is showing changed but the background and other features are not loading" mean exactly?**
   - Are backgrounds/CSS not applying at all? (Check: browser console for CSS errors)
   - Are premium components rendering blank? (Check: JS errors)
   - Are animations not running? (Check: JS errors, prefers-reduced-motion, missing framer-motion)
