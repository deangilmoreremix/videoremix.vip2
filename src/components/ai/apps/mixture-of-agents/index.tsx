import React from "react";
import { GenericAIApp } from "../GenericAIApp";

const mixture_of_agents: React.FC<any> = (props) => {
  return <GenericAIApp {...props} appName="mixture-of-agents" />;
};

export default mixture_of_agents;
