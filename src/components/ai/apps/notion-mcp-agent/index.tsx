import React from "react";
import { GenericAIApp } from "../GenericAIApp";

const notion_mcp_agent: React.FC<any> = (props) => {
  return <GenericAIApp {...props} appName="notion-mcp-agent" />;
};

export default notion_mcp_agent;
